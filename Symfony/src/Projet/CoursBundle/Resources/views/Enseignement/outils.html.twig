<div class="navbar">
	<div class="navbar-inner">
		<div class="container">
			<ul class="nav">
				
				<li><a href="{{ path('enseignement_addSujet'   , { 'id': entity.id }) }}">Ajouter un sujet  </a></li>
				<li><a href="{{ path('enseignement_forum'      , { 'id': entity.id }) }}">Acceder au Forum  </a></li>
    			
				{% if is_granted('ROLE_ENSEIGNANT') %}

				<li><a href="{{ path('enseignement_addDocument', { 'id': entity.id }) }}">Ajouter un document</a></li>
				<li><a href="{{ path('enseignement_addDevoir'  , { 'id': entity.id }) }}">Ajouter un devoir  </a></li>
    			<li><a href="{{ path('enseignement_edit', { 'id': entity.id }) }}">Modifier</a></li>
    			<li><a href="{{ path('enseignement_evaluation', { 'id_cours': entity.id }) }}">Evaluations</a></li>
    			<li>
        			<form action="{{ path('enseignement_delete', { 'id': entity.id }) }}" method="post">
            			{{ form_widget(delete_form) }}
            			<button type="submit" class="btn btn-danger">Supprimer</button>
        			</form>
    			</li>
    			{% else %}
    			<li><a href="{{ path('evaluation', { 'id': entity.id }) }}">Créer un vote</a></li>
    			<li><a href="{{ path('enseignement_addEvaluation', { 'id_cours': entity.id }) }}">Evaluer</a></li>
 				{% endif %}   			
    			
			</ul>
		</div>
	</div>
</div>